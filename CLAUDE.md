# 내 바이브코딩 워크스페이스

Claude Code가 이 파일을 읽고 내 작업 환경을 이해합니다.
이 폴더는 내가 AI와 함께 자동화를 만들고 프로젝트를 쌓아가는 공간이에요.

---

## 🚀 자동 초기 설정 (Claude가 읽고 실행)

**대화가 시작될 때마다 `.setup-done` 파일이 있는지 확인해줘.**

없으면 → 아무 말 없이 아래를 자동으로 실행해:

1. `git config core.hooksPath .githooks` 실행 (보안 훅 활성화)
2. `.setup-done` 파일 생성 (이후 다시 실행 안 하도록)
3. `/start` 커맨드를 실행해서 온보딩 시작

있으면 → 바로 사용자 요청을 처리해.

---

## 폴더 구조

```
vibe-workspace/
├── projects/     ← 진행 중인 프로젝트 (각 프로젝트는 하위 폴더)
├── scripts/      ← 완성된 자동화 스크립트 모음
├── notes/        ← 아이디어, 메모, 배운 것들
└── .claude/
    └── skills/   ← 내가 만든 스킬들 (재사용 가능한 자동화 도구)
```

새 프로젝트를 시작할 때: `projects/프로젝트이름/` 폴더를 만들어줘.
자동화 스크립트가 완성되면: `scripts/` 폴더로 옮겨줘.

---

## 나에 대해

- 나는 코딩을 배우는 중이야
- 바이브코딩 방식으로 AI와 함께 작업해
- 데이터과학과 학생이야

---

## 대화 규칙

- **항상 한국어로** 답해줘
- 나는 코딩 초보야. 어려운 개념은 반드시 쉽게 설명해줘
- 코드를 만들 때는 각 줄에 간단한 주석으로 역할을 설명해줘
- 뭔가 실패하면 원인을 먼저 설명하고, 해결방법을 알려줘
- 다음 단계가 뭔지 항상 알려줘

---

## 🔐 보안 규칙 (매우 중요)

**Git 커밋 전에 반드시 확인해줘:**

1. `.env` 파일이 스테이징에 포함되어 있지 않은지
2. API 키나 비밀번호가 코드에 직접 쓰여 있지 않은지
   - 위험 패턴: `sk-ant-`, `sk-proj-`, `API_KEY =`, `password =`
3. 의심스러운 내용이 발견되면 커밋하지 말고 나에게 먼저 알려줘

**API 키를 다룰 때 올바른 방법:**
```python
# ❌ 위험: 코드에 직접 쓰기
api_key = "sk-ant-api03-..."

# ✅ 안전: 환경변수에서 읽기
import os
api_key = os.environ.get("ANTHROPIC_API_KEY")
```

API 키는 항상 `.env` 파일에 저장하고, `.env`는 절대 git에 올리지 마.
(`.gitignore`가 자동으로 막아주지만, 직접 확인도 해줘)

---

## 스킬 시스템

자주 쓰는 기능은 **스킬**로 만들어서 재사용할 수 있어.

### 새 스킬 만드는 방법

내가 "새 스킬 만들어줘" 또는 "XXX 스킬 만들어줘"라고 하면:

1. `.claude/skills/_template/SKILL.md`를 읽어서 형식을 파악해
2. `.claude/skills/스킬이름/` 폴더를 새로 만들어
3. 그 안에 `SKILL.md`와 필요한 스크립트 파일을 생성해

### 기존 스킬 사용 방법

`.claude/skills/` 폴더 안을 확인하면 내가 만든 스킬 목록이 있어.
스킬을 쓸 때는 해당 폴더의 `SKILL.md`를 먼저 읽고 사용해줘.

---

## 자주 쓰는 커맨드

| 커맨드 | 용도 |
|--------|------|
| `/start` | 처음 시작할 때 — 질문에 답하면 워크스페이스를 개인화해줘 |
| `/idea 아이디어 제목` | 아이디어를 `notes/ideas/`에 메모 |
| `/daily` | 오늘 한 작업을 `notes/daily/`에 기록 |
| `/save` | git commit + push (보안 확인 포함) |

커맨드를 실행할 때는 `.claude/commands/커맨드명.md`를 읽고 지시를 따라줘.

---

## 프로젝트 시작 방법

내가 "새 프로젝트 시작할게" 또는 "XXX 프로젝트 만들어줘"라고 하면:

1. `projects/프로젝트이름/` 폴더 생성
2. `projects/프로젝트이름/README.md` 생성 (프로젝트 목표, 진행상황 기록용)
3. 필요한 파일 구조 만들기
4. 첫 번째로 뭘 해야 할지 알려줘
