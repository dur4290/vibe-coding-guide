---
name: 스킬이름
description: 이 스킬이 하는 일을 한 줄로 설명하세요 (트리거 문구 포함)
allowed-tools: Read, Write, Bash, Glob
---

# 스킬 이름 (여기를 바꾸세요)

> 이 파일은 새 스킬을 만들 때 사용하는 **템플릿**입니다.
> 새 스킬을 만들 때 Claude Code가 이 파일을 읽고 형식을 따릅니다.
>
> 사용법: "XXX 스킬 만들어줘" 라고 하면 Claude가 이 템플릿을 참고해서
> `.claude/skills/xxx/SKILL.md`와 스크립트를 새로 만들어줍니다.

---

## 이 스킬이 하는 일

(한 줄로 설명하세요. 예: "다운로드 폴더를 확장자별로 자동 정리합니다.")

## 사용 방법

Claude Code에서 이렇게 말하면 됩니다:
```
XXX 스킬 써줘
```

또는 직접 실행:
```bash
python .claude/skills/스킬이름/main.py
```

## 필요한 것

- Python 3.x
- 추가 패키지: (없으면 "없음"으로)

## 설정

환경변수나 설정이 필요하면 여기 작성. (없으면 이 섹션 삭제)

```
SOME_CONFIG=값
```

## 예시 출력

```
스킬 실행 시 어떤 결과가 나오는지 예시
```

## 주의사항

- 실행 전에 확인해야 할 것이 있으면 여기 작성
- (없으면 이 섹션 삭제)